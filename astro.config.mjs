// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
	// Custom domain — not registered yet, deployment deferred.
	site: 'https://lindedhauwe.be',
});
